// = failed/postponed/low priority

## emscripten / async transformation
streamline.js does not catch ExitStatus 
asm.js

## core

## UI stuffs
draw cursor
set font
special keys (ctrl etc)
prevent closing window by mistaken ^w

## Web integration
Load vim components (runtime)



## Won't do but patches are accepted:
system clipboard
mouse
tabline
scrollbar
dialog
keypad keys
